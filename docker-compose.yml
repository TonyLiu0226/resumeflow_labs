services:
  resumeflow:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: resumeflow
    ports:
      - "2018:2018"
    environment:
      - NODE_ENV=production
      - PORT=2018
